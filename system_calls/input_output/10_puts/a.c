#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(int argc, char const *argv[])
{

  char str[] = "Hello World";
  puts(str);
  puts(str);
  puts(str);
  puts(str);
  puts(str);
  puts(str);
  return 0;
}
